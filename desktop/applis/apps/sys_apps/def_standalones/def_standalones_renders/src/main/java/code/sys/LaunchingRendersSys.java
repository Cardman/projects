package code.sys;

import code.renders.LaunchingRenders;
import code.sys.impl.ProgramInfos;

public class LaunchingRendersSys extends LaunchingRenders {
    public LaunchingRendersSys() {
        super(new ProgramInfos());
    }
}
