package code.formathtml.exec;

import code.expressionlanguage.opers.LeafOperation;

public final class RendDefaultValueOperation extends RendLeafOperation {
    RendDefaultValueOperation(LeafOperation _l) {
        super(_l);
    }
}
