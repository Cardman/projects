package code.formathtml.render;

import static code.formathtml.EquallableExUtil.assertEq;
import static org.junit.Assert.assertSame;
import static org.junit.Assert.assertTrue;

import org.junit.Test;

import code.sml.DocumentBuilder;
import code.sml.DocumentResult;


public final class MetaDocumentTest {

    @Test
    public void newInstance1Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("Hello World");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(1, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Hello World",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
    }

    @Test
    public void newInstance2Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("Hello<br/>\n");
        doc_.append("World\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(2, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Hello",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaEndLine);
        ch_ = root_.getLastChild();
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        search_ = (MetaPlainLabel) ch_;
        assertEq("World",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(1, search_.getRowGroup());
    }

    @Test
    public void newInstance3Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("Hello<hr/>\n");
        doc_.append("World\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Hello",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaEndLine);
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaSeparator);
        ch_ = root_.getLastChild();
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        search_ = (MetaPlainLabel) ch_;
        assertEq("World",search_.getText());
        assertEq(1, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
    }
    @Test
    public void newInstance4Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance5Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance6Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First</li>");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }

    @Test
    public void newInstance7Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First\n");
        doc_.append("<ol>\n");
        doc_.append("<li>One</li>\n");
        doc_.append("<li>Two</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(3, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = li_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(3, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentNbLabel);
        ch_ = line_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer olSec_ = (MetaContainer) ch_;
        ch_ = olSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer lineSec_ = (MetaContainer) ch_;
        assertEq(2, lineSec_.getChildren().size());
        ch_ = lineSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = lineSec_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("One", lab_.getText());
        ch_ = olSec_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        lineSec_ = (MetaContainer) ch_;
        assertEq(2, lineSec_.getChildren().size());
        ch_ = lineSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = lineSec_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Two", lab_.getText());        
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance8Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First line<br/>\n");
        doc_.append("Next line\n");
        doc_.append("</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(2, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(3, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First line", lab_.getText());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaEndLine);
        ch_ = li_.getLastChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentNbLabel);
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Next line", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance9Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li>First\n");
        doc_.append("<ol>\n");
        doc_.append("<li>One</li>\n");
        doc_.append("<li>Two</li>\n");
        doc_.append("</ol>\n");
        doc_.append("End Sublist\n");
        doc_.append("</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        MetaContainer liSup_ = li_;
        assertEq(3, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = li_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(3, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentNbLabel);
        ch_ = line_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer olSec_ = (MetaContainer) ch_;
        ch_ = olSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer lineSec_ = (MetaContainer) ch_;
        assertEq(2, lineSec_.getChildren().size());
        ch_ = lineSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = lineSec_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("One", lab_.getText());
        ch_ = olSec_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        lineSec_ = (MetaContainer) ch_;
        assertEq(2, lineSec_.getChildren().size());
        ch_ = lineSec_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = lineSec_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Two", lab_.getText());
        MetaComponent innerLine_ = liSup_.getLastChild();
        assertTrue(innerLine_ instanceof MetaLine);
        assertEq(2, ((MetaContainer) innerLine_).getChildren().size());
        MetaComponent ind_ = innerLine_.getFirstChild();
        assertTrue(ind_ instanceof MetaIndentNbLabel);
        innerLine_ = innerLine_.getLastChild();
        assertTrue(innerLine_ instanceof MetaPlainLabel);
        assertEq("End Sublist",((MetaPlainLabel)innerLine_).getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance10Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<table>\n");
        doc_.append("<tr><td>One</td><td>Two</td></tr>\n");
        doc_.append("<tr><td>Three</td><td>Four</td></tr>\n");
        doc_.append("</table>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaTable);
        MetaTable table_ = (MetaTable) ch_;
        assertEq(2, table_.getRemainders().size());
        assertEq(1, table_.getRemainders().first());
        assertEq(3, table_.getRemainders().last());
        assertEq(4, table_.getChildren().size());
        ch_ = table_.getFirstChild();
        assertTrue(ch_ instanceof MetaCell);
        MetaCell td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("One", lab_.getText());
        ch_ = table_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Two", lab_.getText());
        ch_ = table_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Three", lab_.getText());
        ch_ = table_.getLastChild();
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Four", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance11Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<table>\n");
        doc_.append("<caption>Title</caption>\n");
        doc_.append("<tr><td>One</td><td>Two</td></tr>\n");
        doc_.append("<tr><td>Three</td><td>Four</td></tr>\n");
        doc_.append("</table>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaTable);
        MetaTable table_ = (MetaTable) ch_;
        assertEq(3, table_.getRemainders().size());
        assertEq(0, table_.getRemainders().first());
        assertEq(2, table_.getRemainders().get(1));
        assertEq(4, table_.getRemainders().last());
        assertEq(5, table_.getChildren().size());
        ch_ = table_.getFirstChild();
        assertTrue(ch_ instanceof MetaCaption);
        MetaCaption caption_ = (MetaCaption) ch_;
        assertEq(1, caption_.getChildren().size());
        ch_ = caption_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("Title", lab_.getText());
        ch_ = table_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaCell);
        MetaCell td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("One", lab_.getText());
        ch_ = table_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Two", lab_.getText());
        ch_ = table_.getChildren().get(3);
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Three", lab_.getText());
        ch_ = table_.getLastChild();
        assertTrue(ch_ instanceof MetaCell);
        td_ = (MetaCell) ch_;
        assertEq(1, td_.getChildren().size());
        ch_ = td_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(1, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Four", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }

    @Test
    public void newInstance12Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<form n-f='0'>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='0'/>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='1' checked='checked'/>\n");
        doc_.append("</form>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        MetaForm form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        MetaLine formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(2, formLine_.getChildren().size());
        MetaRadioButton radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }

    @Test
    public void newInstance13Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<form n-f='0'>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='0' checked='checked'/>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='1'/>\n");
        doc_.append("</form>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        MetaForm form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        MetaLine formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(2, formLine_.getChildren().size());
        MetaRadioButton radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }

    @Test
    public void newInstance14Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<form n-f='0'>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='0'/>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='1'/>\n");
        doc_.append("</form>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        MetaForm form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        MetaLine formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(2, formLine_.getChildren().size());
        MetaRadioButton radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }
    @Test
    public void newInstance15Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<form n-f='0'>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='0'/>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='1' checked='checked'/>\n");
        doc_.append("<input type='radio' name='myradio2' n-i='1' value='2'/>\n");
        doc_.append("<input type='radio' name='myradio2' n-i='1' value='3' checked='checked'/>\n");
        doc_.append("</form>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        MetaForm form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        MetaLine formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(4, formLine_.getChildren().size());
        MetaRadioButton radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(2);
        assertEq(0,radio_.getId().getForm());
        assertEq(1,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(3);
        assertEq(0,radio_.getId().getForm());
        assertEq(1,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }
    @Test
    public void newInstance16Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<form n-f='0'>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='0'/>\n");
        doc_.append("<input type='radio' name='myradio' n-i='0' value='1' checked='checked'/>\n");
        doc_.append("</form>\n");
        doc_.append("<form n-f='1'>\n");
        doc_.append("<input type='radio' name='myradio2' n-i='0' value='2'/>\n");
        doc_.append("<input type='radio' name='myradio2' n-i='0' value='3' checked='checked'/>\n");
        doc_.append("</form>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(5, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        MetaForm form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        MetaLine formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(2, formLine_.getChildren().size());
        MetaRadioButton radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(0,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        assertTrue(radio_.isChecked());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(3);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        form_ = (MetaForm) cont_.getChildren().get(0);
        assertEq(1, form_.getChildren().size());
        formLine_ = (MetaLine)form_.getChildren().get(0);
        assertEq(2, formLine_.getChildren().size());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(0);
        assertEq(1,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(!radio_.isChecked());
        radio_ = (MetaRadioButton)formLine_.getChildren().get(1);
        assertEq(1,radio_.getId().getForm());
        assertEq(0,radio_.getId().getInput());
        assertTrue(radio_.isChecked());
        ch_ = root_.getChildren().get(4);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }
    @Test
    public void newInstance17Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<span>");
        doc_.append("Inner");
        doc_.append("</span>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(1, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Inner",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
    }
    @Test
    public void newInstance18Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>");
        doc_.append("<body>");
        doc_.append("<div>\n");
        doc_.append("Inner\n");
        doc_.append("</div>");
        doc_.append("</body>");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getChildren().get(0);
        assertEq(1, ((MetaImageMap)ch_).getChildren().size());
        MetaPlainLabel search_ = (MetaPlainLabel) ((MetaImageMap)ch_).getChildren().get(0);
        assertEq("Inner",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }
    @Test
    public void newInstance19Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<span>");
        doc_.append("Two  spaces");
        doc_.append("</span>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(1, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Two spaces",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
    }
    @Test
    public void newInstance20Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<span>");
        doc_.append("Two <span/>spaces");
        doc_.append("</span>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(1, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel search_ = (MetaPlainLabel) ch_;
        assertEq("Two ",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
        ch_ = cont_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaPlainLabel);
        search_ = (MetaPlainLabel) ch_;
        assertEq("spaces",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
    }
    @Test
    public void newInstance21Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ul>\n");
        doc_.append("<li><span>First</span></li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ul>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaUnorderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance22Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>");
        doc_.append("<body>");
        doc_.append("<p>");
        doc_.append("Inner");
        doc_.append("</p>");
        doc_.append("</body>");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getChildren().get(0);
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(1, cont_.getChildren().size());
        ch_ = cont_.getChildren().get(0);
        assertEq(3, ((MetaParagraph)ch_).getChildren().size());
        MetaLine line_ = (MetaLine)((MetaParagraph)ch_).getChildren().get(0);
        assertEq(1, line_.getChildren().size());
        assertTrue(line_.getChildren().get(0) instanceof MetaEndLine);
        line_ = (MetaLine)((MetaParagraph)ch_).getChildren().get(1);
        assertEq(1, line_.getChildren().size());
        MetaPlainLabel search_ = (MetaPlainLabel) line_.getChildren().get(0);
        assertEq("Inner",search_.getText());
        assertEq(0, search_.getPartGroup());
        assertEq(0, search_.getRowGroup());
        line_ = (MetaLine)((MetaParagraph)ch_).getChildren().get(2);
        assertEq(1, line_.getChildren().size());
        assertTrue(line_.getChildren().get(0) instanceof MetaEndLine);
        ch_ = root_.getChildren().get(2);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
    }
    @Test
    public void newInstance23Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='i'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("i", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("ii", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance24Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='a'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("a", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("b", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance25Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='I'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("I", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("II", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance26Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='A'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("A", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("B", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance27Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li type='1'>First</li>\n");
        doc_.append("<li type='disc'>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        assertSame(MetaPointForm.DISK,((MetaPointLabel)ch_).getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance28Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li type='square'>First</li>\n");
        doc_.append("<li type='rect'>Second</li>\n");
        doc_.append("<li type='circle'>Third</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.SQUARRE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.RECT, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.CIRCLE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance29Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li type='I'>First</li>\n");
        doc_.append("<li type='i'>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("I", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("ii", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance30Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol>\n");
        doc_.append("<li type='A'>First</li>\n");
        doc_.append("<li type='a'>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("A", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("b", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance31Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='1'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance32Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='square'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("<li>Third</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.SQUARRE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.SQUARRE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.SQUARRE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance33Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='disc'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("<li>Third</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance34Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='circle'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("<li>Third</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.CIRCLE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.CIRCLE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.CIRCLE, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance35Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='rect'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("<li>Third</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.RECT, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.RECT, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.RECT, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance36Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ul type='ul'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("<li>Third</li>\n");
        doc_.append("</ul>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaUnorderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(3, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        MetaPointLabel nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaPointLabel);
        nb_ = (MetaPointLabel) ch_;
        assertSame(MetaPointForm.DISK, nb_.getForm());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Third", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
    @Test
    public void newInstance37Test() {
        StringBuilder doc_ = new StringBuilder();
        doc_.append("<html>\n");
        doc_.append("<body>\n");
        doc_.append("<ol type='ol'>\n");
        doc_.append("<li>First</li>\n");
        doc_.append("<li>Second</li>\n");
        doc_.append("</ol>\n");
        doc_.append("</body>\n");
        doc_.append("</html>");
        DocumentResult res_ = DocumentBuilder.newDocumentBuilder().parse(doc_.toString());
        MetaDocument out_ = MetaDocument.newInstance(res_.getDocument());
        MetaBlock root_ = out_.getRoot();
        assertEq(3, root_.getChildren().size());
        MetaComponent ch_ = root_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer cont_ = (MetaContainer) ch_;
        assertEq(0, cont_.getChildren().size());
        ch_ = root_.getChildren().get(1);
        assertTrue(ch_ instanceof MetaLine);
        cont_ = (MetaContainer) ch_;
        assertEq(2, cont_.getChildren().size());
        ch_ = cont_.getFirstChild();
        assertTrue(ch_ instanceof MetaIndentLabel);
        ch_ = cont_.getLastChild();
        assertTrue(ch_ instanceof MetaOrderedList);
        MetaContainer ol_ = (MetaContainer) ch_;
        assertEq(2, ol_.getChildren().size());
        ch_ = ol_.getFirstChild();
        assertTrue(ch_ instanceof MetaListItem);
        MetaContainer li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        MetaContainer line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        MetaNumberedLabel nb_ = (MetaNumberedLabel) ch_;
        assertEq("1", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        MetaPlainLabel lab_ = (MetaPlainLabel) ch_;
        assertEq("First", lab_.getText());
        ch_ = ol_.getLastChild();
        assertTrue(ch_ instanceof MetaListItem);
        li_ = (MetaContainer) ch_;
        assertEq(1, li_.getChildren().size());
        ch_ = li_.getFirstChild();
        assertTrue(ch_ instanceof MetaLine);
        line_ = (MetaContainer) ch_;
        assertEq(2, line_.getChildren().size());
        ch_ = line_.getFirstChild();
        assertTrue(ch_ instanceof MetaNumberedLabel);
        nb_ = (MetaNumberedLabel) ch_;
        assertEq("2", nb_.getNumber());
        ch_ = line_.getLastChild();
        assertTrue(ch_ instanceof MetaPlainLabel);
        lab_ = (MetaPlainLabel) ch_;
        assertEq("Second", lab_.getText());
        ch_ = root_.getChildren().last();
        assertTrue(ch_ instanceof MetaLine);
    }
}
